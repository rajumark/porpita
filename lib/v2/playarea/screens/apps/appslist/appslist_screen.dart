import 'package:flutter/material.dart';

class AppsListScreen extends StatelessWidget {
  const AppsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('coming soon appslist'),
    );
  }
}
